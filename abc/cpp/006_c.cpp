#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
using ll = long long;

int main() {
  int n;
  cin >> n;
  int ans = 0, w;
  vector<vector<int>> vv;
  rep(i, n) {
    cin >> w;
    if (vv.size() == 0) {
      vv.push_back({w});
      ans++;
      continue;
    }

    //
  }

  return 0;
}
